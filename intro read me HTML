import pyttsx3

text = """Core belongs to the author.
Leaf belongs to all stewards.
Captain’s Log carries the lineage.
Protocols and Archives are communal gifts."""
engine = pyttsx3.init()
engine.say(text)
engine.runAndWait()
